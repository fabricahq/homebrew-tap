# Compatibility endpoint used by the existing Code Rules release trigger.
name: Update Code Rules
on:
  workflow_dispatch:
permissions:
  contents: read
  issues: write
jobs:
  update:
    uses: ./.github/workflows/update-tool.yml
    with:
      tool: code-rules
